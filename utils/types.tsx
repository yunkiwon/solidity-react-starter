export type githubInfo = {
    full_name: string;
    html_url: string;
    description: string;
    size: number;
};

export type ensInfo = {
    name: string;
};

export type mirrorInfo = {
    id: string,
    title: string,
    body: string,
    timestamp: number,
    projectName: string,
    projectDescription: string,
};
